% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mdfa-cov.R
\name{MDFA_cov}
\alias{MDFA_cov}
\title{Matrix Decomposition Factor Analysis for a covariance matrix}
\usage{
MDFA_cov(
  covar,
  nfactors = 3,
  tol = 1e-05,
  maxit = 1000,
  SMC = TRUE,
  trace = 1,
  REPORT = 1,
  rotate = "varimax",
  n.rotations = 1,
  hyper = 0.15,
  uniq_range = c(0.2, 1),
  alg = "ALS",
  ...
)
}
\arguments{
\item{covar}{A covariance matrix.}

\item{nfactors}{Number of factors.}

\item{tol}{The absolute convergence tolerance.}

\item{maxit}{The maximum number of iterations.}

\item{SMC}{If \code{TRUE}, use squared multiple correlations as the initial
communality estimate. If \code{FALSE}, initial unique variances are drawn
randomly from \code{uniq_range}.}

\item{trace}{Non-negative integer. If positive, tracing information on
the progress of the optimization is produced.}

\item{REPORT}{Reporting frequency: progress is printed every \code{REPORT}
iterations when \code{trace > 0}.}

\item{rotate}{Rotation method applied to the loadings. One of \code{"none"},
\code{"varimax"}, \code{"Varimax"}, \code{"quartimax"}, \code{"bentlerT"}, \code{"targetT"},
\code{"TargetT"}, or \code{"equamax"}. Rotations other than \code{"varimax"} and
\code{"none"} require the \pkg{GPArotation} package.}

\item{n.rotations}{If greater than 1, the rotation is instead performed
\code{n.rotations} times from random orthogonal starting configurations via
\code{\link[psych]{faRotations}} (borrowed from the \pkg{psych}
package), and the best solution (by hyperplane count) is kept. This can
help avoid poor local optima in the rotation step. Requires the
\pkg{psych} package.}

\item{hyper}{Hyperplane count threshold passed to
\code{\link[psych]{faRotations}} when \code{n.rotations > 1}; unused
otherwise.}

\item{uniq_range}{Interval from which random initial unique variances are
drawn when \code{SMC = FALSE}.}

\item{alg}{Estimation algorithm: \code{"ALS"} for Alternating Least Squares, or
\code{"BFGS"} for a quasi-Newton gradient-based optimizer (via \code{\link[stats:optim]{stats::optim()}}).}

\item{...}{Additional arguments passed on to the rotation function.}
}
\value{
A list with elements:
\describe{
\item{loadings}{A matrix of loadings, one column per factor.}
\item{uniquenesses}{\code{diag(covar - model)}; note this differs from the
unique variances estimated directly by MDFA, which are not returned
by this function.}
\item{communality, communalities}{Communalities implied by the model.}
\item{e.values}{Eigenvalues of \code{covar}.}
\item{model}{The model-implied covariance matrix, \verb{loadings \\\%*\\\% t(loadings)}.}
\item{rotation}{The rotation method used.}
\item{rot.mat}{The rotation matrix, if a rotation was applied.}
\item{fm}{Always \code{"mdfa"}; identifies the factoring method.}
}
}
\description{
Performs Matrix Decomposition Factor Analysis (MDFA) for a given
covariance matrix. Unlike \code{\link[=MDFA]{MDFA()}}, this function does not perform
bootstrap replications and therefore does not provide standard errors
for the estimated loadings. Loadings and unique
variances are estimated from a data matrix (or its sample covariance
matrix) by minimizing a discrepancy between the data and the model,
using either an Alternating Least Squares (ALS) algorithm (see Adachi
and Trendafilov (2018)) or a quasi-Newton (BFGS)
algorithm. Here, we note that the ALS algorithm proposed by Adachi (2012) is employed.
Thus, the common and unique factor scores are not estimated.
}
\references{
Adachi, K. (2012). Some contributions to data-fitting factor analysis
with empirical comparisons to covariance-fitting factor analysis.
Journal of the Japanese Society of Computational Statistics, 25, 25-38.
\doi{10.5183/jjscs.1106001_197}

Adachi, K. and Trendafilov, N. T. (2018). Some mathematical properties of
the matrix decomposition solution in factor analysis. Psychometrika, 83,
407-424. \doi{10.1007/s11336-017-9600-y}
}
